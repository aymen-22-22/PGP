# Where the API lives. Leave unset in development: Vite proxies /api to the
# backend. In production set it only if the API is on another origin.
# VITE_API_URL=https://erp.example.com/api/v1

# Dev-only: where the Vite proxy forwards /api during `npm run dev`.
VITE_DEV_API=http://localhost:3000
